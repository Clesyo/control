package com.app.control.api.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import com.app.control.models.Ingredient;
import com.app.control.repository.IngredientRepository;

@Controller
public class IngredientController {

	/*
	 * @Autowired private IngredientRepository ir;
	 * 
	 * @RequestMapping(value = "/ingredientes", method = RequestMethod.GET) public
	 * String index() { return "ingredient/ingredient_index"; }
	 * 
	 * @RequestMapping(value = "/ingredientes", method = RequestMethod.POST) public
	 * String store(Ingredient ingredient) { ir.save(ingredient); return
	 * "redirect:/ingredientes"; }
	 */
}
